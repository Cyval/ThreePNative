import { LOGIN } from '../actions/actionTypes';
const initialState = {
  userName: 'sonjay'
};

export function login(state = initialState, action){
  switch(action.type){
    default :
      return state;
  }
};